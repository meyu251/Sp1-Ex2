#include <stdio.h>


int knapSack (int weights[], int values[] , int selected_bool[]){


}


int main(){

    char products[5];
    int values[5] = {0};
    int weights[5] = {0};
    int isSelect[5] = {0};
    int i;

    printf("enter producs, values and weights:\n");
    printf("(product, value, weight)\n");
    for(i=0; i<5; i++)
    {
        scanf("%c, %d, %d", &products[i], &values[i], &weights[i]);
    }
    
    // my change

    


    return 0;
}